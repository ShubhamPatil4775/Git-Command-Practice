name: scheduled time workflow 

# on:
#   schedule:
#     - cron: "* * * * *"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Print build
        run: echo "Build"
        
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Print test
        run: echo "tested"
        
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Print deploy
        run: echo "deployed."
